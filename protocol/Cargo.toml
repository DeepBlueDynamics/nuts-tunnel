[package]
name = "nuts-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared protocol types for nuts-tunnel"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
